// src/components/MemeDisplay.js
import React from 'react';

function MemeDisplay({ meme }) {
  if (!meme) return null;

  return (
    <div className="meme-display">
      <img src={meme.image} alt="Created meme" />
      <p>{meme.text}</p>
    </div>
  );
}

export default MemeDisplay;
